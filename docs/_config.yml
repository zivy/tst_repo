remote_theme: zivy/cayman
defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
      hero_image: ibex_banner.png
      site.fixed_navbar: bottom

